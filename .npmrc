# Rently — Secure npm configuration
# Prevents malicious postinstall scripts from running during npm install
# In CI, use: npm ci --ignore-scripts then run postinstall manually

# Use consistent lockfile format
package-lock=true

# Audit on install (warns about known vulnerabilities)
audit=true

# Automatically review installs
fund=false
